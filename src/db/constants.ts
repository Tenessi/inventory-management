export enum INVENTORY_MANAGEMENT_TABLES {
  USERS = 'users',
  TRANSACTIONS = 'transactions',
  PRODUCTS = 'products',
  ACCOUNTING_RECORDS = 'accountingRecords',
  WAREHOUSES = 'warehouses',
  WAREHOUSE_PRODUCTS = 'warehouseProducts',
}
